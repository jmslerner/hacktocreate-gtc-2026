fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0
aiofiles==23.2.1

# Video processing
ffmpeg-python==0.2.0

# GPU / Image generation
# Dell GB10 (Blackwell B10 + Grace ARM): install PyTorch with CUDA 12.8 support
# Run this first if not already installed:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.5.0
torchvision>=0.20.0
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.3
Pillow>=10.3.0

# PDF export (production report)
reportlab==4.2.2
